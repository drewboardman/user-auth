TODO
----
* need to add encrypted client identifier and decryption
* add redis for sessions
* docker-compose for redis and psql
* github actions
* add jwt auth endpoint that just passes to the google authenticator
* add ciris and configs
